<Page x:Class="TransportCompany.PageMain.PageLogin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TransportCompany.PageMain"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Авторизация">

    <Grid>
        <StackPanel Grid.Row="1" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <Label Content="Логин:" Style="{StaticResource Label}"/>
                <TextBox x:Name="TxtBLogin" Style="{StaticResource TextBox}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Пароль:" Style="{StaticResource Label}"/>
                <PasswordBox x:Name="psbPassword" Style="{StaticResource PasswordBox}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="Login" Content="Войти" Style="{StaticResource ButtonLog}" Click="Login_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
